{
  "name": "@mizant/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@mizant/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.17.2",
    "tsx": "4.20.4",
    "vitest": "3.2.7"
  }
}
